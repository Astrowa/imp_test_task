select round(sum(spend) / sum(conversions), 2) from {{ ref('src_ads_tiktok_ads_all_data')}}
group by channel
;

select sum(spend) / sum(conv) from {{ref('src_ads_bing_all_data')}}
group by channel
;


select sum(spend) / sum(purchase) from {{ref('src_ads_creative_facebook_all_data')}}
group by channel
;


select distinct 0 as therearenoconv from {{ref('src_promoted_tweets_twitter_all_data')}}
group by channel
;


select * from {{ref('mcdm_paid_ads_basic_performance_structure')}};